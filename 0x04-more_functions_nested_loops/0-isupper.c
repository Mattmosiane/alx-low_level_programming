#include "main.h"

/**
 * _isupper - Check if the letter is upper
 * @x: Nuber to be checked
 * Return: 1 for upper letter of 0 for any else
 */

int _isupper(int x)

{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
